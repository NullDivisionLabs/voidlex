<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!--
        QUERY_ALL_PACKAGES is required to populate the per-app routing
        picker (Settings → App Routing). The user explicitly opts into
        a "proxy these / bypass these" list of installed packages; without
        full visibility we'd silently miss every app the user expects to
        toggle. The standard <queries> elements only cover narrow intent
        filters and can't enumerate arbitrary installed apps.

        Play Store policy classifies this as a sensitive permission. When
        submitting a VPN app the declaration form requires:
          - app category: "VPN/Connectivity"
          - feature requiring it: "Allow user to choose which apps to
            route through (or exclude from) the VPN tunnel"
        See https://support.google.com/googleplay/android-developer/answer/10158779
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!--
        Google TV / Android TV support. The Leanback feature is optional so
        the same APK installs on phones and TV; the LEANBACK_LAUNCHER intent
        filter below makes the app appear on the TV home screen, the banner
        meta-data supplies the artwork Leanback requires.
    -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.faketouch" android:required="false" />

    <application
        android:label="Void//Lex"
        android:name=".VoidLexApplication"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Surfaces the app on the Google TV / Android TV home row. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
            <!-- Encrypted subscription codes: clicking a voidlex:// link
                 in a browser/messenger opens the app, which decrypts the
                 blob and imports the underlying subscription. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="voidlex"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- VPN Service Definition -->
        <service
            android:name=".VoidVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:foregroundServiceType="specialUse"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <property 
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" 
                android:value="VPN service for secure tunneling" />
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="true" />
        </service>
        <!-- Standalone local SOCKS5/HTTP proxy. Runs Xray as a regular
             foreground service without VpnService.Builder, so Android
             does not display the system VPN key icon. -->
        <service
            android:name=".VoidProxyService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Local SOCKS5/HTTP proxy" />
        </service>
        <!--
            Transparent, no-UI activity that handles widget clicks.
            Going through an Activity (rather than letting AppWidgetProvider
            startForegroundService directly from onReceive) ensures the VPN
            service is started from a foreground context, which is required
            on some OEM stacks for the new tun0 to be promoted as the
            default route for other apps. See [WidgetActionActivity] for
            the full rationale.
        -->
        <activity
            android:name=".WidgetActionActivity"
            android:theme="@android:style/Theme.NoDisplay"
            android:exported="false"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="true"
            android:launchMode="singleInstance"
            android:taskAffinity=""/>
        <service
            android:name=".VoidLexTileService"
            android:label="@string/quick_settings_tile_label"
            android:icon="@drawable/ic_quick_settings_tile"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>
        <receiver
            android:name=".VoidLexCompactAppWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label_compact">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/void_lex_widget_compact_info" />
        </receiver>
        <receiver
            android:name=".VoidLexGlobalProxyAppWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label_global_proxy">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/void_lex_widget_global_proxy_info" />
        </receiver>
        <receiver
            android:name=".VoidLexWideAppWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label_wide">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/void_lex_widget_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher needs explicit visibility on Android 11+ to resolve
             https:// targets (Telegram channel link in the About footer). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
